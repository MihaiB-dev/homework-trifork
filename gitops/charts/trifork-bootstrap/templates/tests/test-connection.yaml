apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trifork-bootstrap.fullname" . }}-test-connection"
  labels:
    {{- include "trifork-bootstrap.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trifork-bootstrap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
